import 'package:flutter/material.dart';

class StarredView extends StatelessWidget {
  const StarredView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Starred View'),
    );
  }
}
